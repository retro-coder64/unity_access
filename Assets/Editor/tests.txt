NVDA controller live test: nvdaController_testIfRunning and nvdaController_speakText both returned 0 with NVDA 2025.3.3 running. Validate the hierarchy view in Unity: Start and H open it, Up and Down speak scene objects, and Enter publishes the selected object through SharedSelection. Inspector manual validation: Enter in the hierarchy opens the accessible inspector; Up and Down speak all six transform axes and attached components; Enter edits numeric transform values with Undo support; invalid values are announced; Escape cancels an active edit, and Escape outside edit mode closes the inspector before a deferred callback focuses the hierarchy and announces the current hierarchy item. Shared utility validation: verify all lists retain selection highlighting and NVDA position announcements, Clear Console activates by mouse and Enter, inspector text boxes commit with Enter and cancel with Escape, object search remains focused while filtering, and all windows log speech failures to Assets/Editor/debug.txt.
Unity 6.3.22f1 compilation validation: ProjectSettingsAccessibilityWindow.cs, ProjectSettingsTagManager.cs, and BuildProfilesAccessibilityWindow.cs compile without C# errors. Manual NVDA validation: Project Settings exposes only Player and Tags and Layers; Up and Down or Tab announce each control, value, checked and disabled state; Enter edits or activates; Escape returns; Fullscreen Mode refreshes dependent controls without losing the selected control; tag removal is confirmed; sorting layers can be renamed, removed, and moved without dragging; only user layer slots are editable. Build Profiles announces the active target; reports missing Windows Build Support with the Unity Hub route; returns from a custom profile to the Windows platform profile; manages the global Scene List by keyboard; exposes architecture, Local Machine, Development Build, and compression; and announces succeeded, failed, or cancelled build results with available output details.
